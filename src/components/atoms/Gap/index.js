import React from 'react'
import { View } from 'react-native'

const Gap = ({heigth, width}) => {
    return (
        <View style={{ height : heigth, width :width }}>
        </View>
    )
}
export default Gap